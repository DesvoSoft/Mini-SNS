<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Write - Mini SNS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google font component class:font-body-gamer -->
    <%- include('components/_gfont.ejs') %> <%-
    include('components/_ui-helpers') %>
    <link rel="stylesheet" href="/css/bg.css" />
    <!-- <link rel="stylesheet" href="/css/write.css" /> -->
  </head>
  <body class="app-body">
    <%- include('components/_header', { username: username }) %>

    <main class="page-shell page-shell--narrow">
      <h1 class="page-title">Write a New Post</h1>

      <div class="action-row action-row--left">
        <a href="/" class="btn btn-secondary"> ← Back to Home </a>
      </div>

      <%- include('components/_categories', { categories: categories }) %>

      <div class="content-card">
        <form action="/posts" method="post" class="space-y-6">
          <div>
            <label for="writer" class="input-label">Posting as:</label>
            <p class="input-ghost"><%= username %></p>
            <input type="hidden" name="writer" value="<%= username %>" />
          </div>

          <div>
            <label for="content" class="input-label">Your Post:</label>
            <textarea
              id="content"
              name="content"
              rows="5"
              placeholder="Hello, lets post!"
              required
              class="textarea-primary"
            ></textarea>
          </div>

          <div>
            <label for="privacy" class="input-label">Privacy:</label>
            <select name="privacy" id="privacy" class="input-field">
              <option value="public">Public (Everyone)</option>
              <option value="friends">Friends Only</option>
              <option value="private">Private (Only Me)</option>
            </select>
          </div>

          <button type="submit" class="btn-success">Post</button>
        </form>
      </div>
    </main>

    <%- include('components/_footer') %>
  </body>
</html>
