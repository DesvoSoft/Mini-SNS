<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Mini SNS - Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <%- include('components/_gfont.ejs') %>
    <%- include('components/_ui-helpers') %>
    <!-- <link rel="stylesheet" href="/css/global.css" /> -->
    <link rel="stylesheet" href="/css/bg.css" />
    <style>
      @layer utilities {
        .text-shadow {
          text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }
        .text-shadow-md {
          text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.4);
        }
        .text-shadow-lg {
          text-shadow: 3px 3px 4px rgba(0, 0, 0, 0.7);
        }
        .text-wshadow-lg {
          text-shadow: 1px 2px 4px rgba(255, 255, 255, 0.7);
        }
      }
    </style>
  </head>
  <body class="app-body">
    <%- include('components/_header', { username: username }) %>

    <main class="page-shell page-shell--narrow space-y-6">
      <h1 class="page-title">
        Welcome to Mini SNS<% if (username) { %>, <%= username %> <% } %>
      </h1>

      <!-- Error si usuario o contraseña son incorrectos -->
      <% if (loginError) { %>
      <p class="text-red-300 font-semibold text-center mb-4"><%= loginError %></p>
      <% } %>

      <div class="content-card space-y-3 text-center">
        <p class="font-body-gamer text-lg">
          Social media playground, now using
          <span class="text-cyan-200 text-shadow-lg animate-pulse">Tailwind</span>
        </p>
        <p class="text-sm text-gray-300">
          Usa las mismas cabeceras, pies de página y helpers en todas las vistas
          para mantener un estilo consistente.
        </p>
        <div class="flex justify-center">
          <a href="/docs" class="btn btn-primary">View Documentation</a>
        </div>
      </div>

      <% if (!username) { %>
      <div class="content-card">
        <h2 class="text-2xl font-semibold text-center mb-6">Login</h2>
        <form
          method="POST"
          action="/login"
          class="flex flex-col gap-4 max-w-md mx-auto"
        >
          <label for="username" class="input-label">Username</label>
          <input
            type="text"
            name="username"
            placeholder="Enter username"
            required
            class="input-field font-mono"
            autofocus
          />

          <label for="password" class="input-label">Password</label>
          <input
            type="password"
            name="password"
            placeholder="Enter password"
            required
            class="input-field font-mono"
          />

          <button type="submit" class="btn-success">Login</button>
        </form>
      </div>
      <% } %> <% if (username) { %>
      <h2 class="text-center text-xl font-semibold">Available</h2>

      <div class="action-row">
        <a href="/write" class="btn btn-primary">Write a Post</a>
        <a href="/posts" class="btn btn-primary">View Posts</a>
        <a href="/docs" class="btn btn-primary">Documentation</a>
        <a href="/profile" class="btn btn-primary">Profile</a>
      </div>
      <% } else { %>
      <h2 class="text-center text-xl mt-8 mb-4">Log in required</h2>
      <% } %>
    </main>
    <%- include('components/_footer') %>
  </body>
</html>
